# Roadmap

本路线图记录知档的近期和中期开发方向。它用于帮助维护者控制范围和确定优先级，不代表功能已经完成，也不构成发布时间承诺。

## 当前原则

知档只解决一个问题：**完整、安全、可靠地导出作者自己的知乎回答和文章，并保存为开放、可验证、可长期使用的本地归档。**

网站生成、网站部署、内容编辑、版本管理、AI 分类和个人写作模型都不属于知档，由独立项目承担。本路线图只包含把归档这一层做稳所需的工作。

## 阶段 1：稳定 Archive Core

**状态：当前优先级**

目标是把知乎内容导出能力做成可以长期维护和反复验证的核心。

### 可靠性

- 正确处理回答和文章的分页；
- 区分接口报告总数、分页实际返回数和去重后的唯一内容数；
- 对重复记录进行去重并明确报告；
- 对无法解释的数量差异给出完整性警告；
- 在接口结构发生变化时明确停止或降级，而不是静默生成错误数据；
- 对单篇内容和图片失败进行可追踪报告；
- 避免已有归档被意外覆盖。

### 数据完整性

持续完善并稳定以下归档内容：

- Markdown 正文；
- `index.json`；
- `export-report.json`；
- 原文链接；
- 创建和更新时间；
- 问题 ID；
- 点赞、评论和可用的收藏数据；
- 封面；
- 本地化图片。

`index.json` 和 Markdown 应被视为长期数据格式。破坏兼容性的修改必须经过明确的 schema 版本设计。

### 登录与第三方变化

- 正确处理浏览器被关闭、登录失效和重新登录；
- 对知乎正常安全验证给出明确提示；
- 不实现 [ACCEPTABLE_USE.md](ACCEPTABLE_USE.md) 排除的能力（验证码绕过、访问控制规避、代理池、账号池、限流规避）；
- 减少无必要的第三方请求；
- 对接口字段变化保留可测试、可维护的降级路径。

### 隐私与安全

- 密码不进入应用数据；
- Cookie、浏览器 Profile 和真实导出内容不进入 Git 仓库；
- 本地服务继续只监听 `127.0.0.1`；
- 导出数据不默认加入作者姓名、简介、账号标识等不必要个人资料；
- 测试只使用合成数据；
- 不增加遥测、远程日志或用户内容上传。

### 验证标准

核心导出进入稳定状态前，应逐步做到：

1. 自动测试覆盖关键分页、去重、图片和目录安全场景；
2. `npm test` 和 `npm run build` 持续通过；
3. 在干净环境中验证完整导出；
4. 对已知失败模式有明确的错误信息和故障排查文档；
5. 导出报告能够区分完整成功、可解释重复、部分失败和完整性风险。

## 阶段 2：降低安装和使用门槛

**状态：已完成，1.0.0 已发布**

0.3.x 版本需要用户准备 Node.js、npm、Git 和 Chrome。`v1-tauri` 分支已经把应用重写为基于 [Tauri](https://tauri.app) 的独立桌面应用：知乎登录改为内嵌窗口完成，后端用 [Bun](https://bun.sh) 编译成独立可执行文件随应用打包，普通用户不再需要安装 Node.js、Chrome 或运行任何命令。目标用户流程已经落地：

```text
下载应用
→ 打开应用
→ 点击开始登录
→ 确认保存位置
→ 开始导出
```

### 这一阶段的硬要求

- 不降低 Archive Core 的完整性标准；已完成——归档、去重、图片处理逻辑与 0.3.x 完全一致，只是登录和分页请求的底层实现从 Playwright 换成了内嵌 WebView；
- 登录仍在用户本机完成；已完成；
- 登录状态只保存在本机（由系统 WebView 管理）；已完成；
- 用户能够清除保存的登录数据；已完成——应用内置"退出登录"；
- 不因为桌面化引入云端账号、遥测或用户内容上传；已完成；
- macOS 和 Windows 至少完成干净环境测试。**部分完成**——目前只在 macOS（Apple Silicon）上做过完整测试；1.0.0 以 macOS-only 发布，Windows、Linux 和 Intel Mac 留到后续版本。

`v1-tauri` 已合并到 `main`，[1.0.0 已正式发布](https://github.com/zhangyingfeng/zhi-dang/releases)。

### 1.0.0 之后

- Windows / Intel Mac / Linux 平台测试；
- Apple 开发者签名 + 公证，消除首次打开时的 Gatekeeper 警告（目前用免费的 ad-hoc 签名，警告可以在系统设置里手动绕过，不阻塞使用，但不如正式签名顺滑）；
- 导出体验从进度条改为可选择、可暂停续传的任务列表，仍在评估中。

## 范围控制

知档只做本机归档。以下方向不属于知档，由独立项目承担，不在本路线图内：

- 从归档生成个人网站，以及网站托管、域名和 DNS；
- 内容编辑、草稿和发布流程；
- 文章版本历史和差异比较；
- CMS 和云同步；
- AI 分类、摘要和检索；
- 个人写作模型。

新增功能进入实现前，应先回答一个问题：

> 这个修改是否首先提高了作者对自己内容的完整保存、长期可用性，或降低了使用门槛？

如果不是，它就不属于知档。
