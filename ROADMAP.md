# Roadmap

本路线图记录知档的近期和中期开发方向。它用于帮助维护者控制范围和确定优先级，不代表功能已经完成，也不构成发布时间承诺。

## 当前原则

项目当前首先解决一个问题：**完整、安全、可靠地导出作者自己的知乎回答和文章，并保存为开放、可验证、可长期使用的本地归档。**

在核心导出能力稳定之前，不把网站生成、网站部署、内容编辑、AI 分类或个人写作模型作为当前开发重点。

## 阶段 1：稳定 Archive Core

**状态：当前优先级**

目标是把知乎内容导出能力做成可以长期维护和反复验证的核心。

### 可靠性

- 正确处理回答和文章的分页；
- 区分接口报告总数、分页实际返回数和去重后的唯一内容数；
- 对重复记录进行去重并明确报告；
- 对无法解释的数量差异给出完整性警告；
- 在接口结构发生变化时明确停止或降级，而不是静默生成错误数据；
- 对单篇内容和图片失败进行可追踪报告；
- 避免已有归档被意外覆盖。

### 数据完整性

持续完善并稳定以下归档内容：

- Markdown 正文；
- `index.json`；
- `export-report.json`；
- 原文链接；
- 创建和更新时间；
- 问题 ID；
- 点赞、评论和可用的收藏数据；
- 封面；
- 本地化图片。

`index.json` 和 Markdown 应被视为长期数据格式。破坏兼容性的修改必须经过明确的 schema 版本设计。

### 登录与第三方变化

- 正确处理浏览器被关闭、登录失效和重新登录；
- 对知乎正常安全验证给出明确提示；
- 不实现 [ACCEPTABLE_USE.md](ACCEPTABLE_USE.md) 排除的能力（验证码绕过、访问控制规避、代理池、账号池、限流规避）；
- 减少无必要的第三方请求；
- 对接口字段变化保留可测试、可维护的降级路径。

### 隐私与安全

- 密码不进入应用数据；
- Cookie、浏览器 Profile 和真实导出内容不进入 Git 仓库；
- 本地服务继续只监听 `127.0.0.1`；
- 导出数据不默认加入作者姓名、简介、账号标识等不必要个人资料；
- 测试只使用合成数据；
- 不增加遥测、远程日志或用户内容上传。

### 验证标准

核心导出进入稳定状态前，应逐步做到：

1. 自动测试覆盖关键分页、去重、图片和目录安全场景；
2. `npm test` 和 `npm run build` 持续通过；
3. 在干净环境中验证完整导出；
4. 对已知失败模式有明确的错误信息和故障排查文档；
5. 导出报告能够区分完整成功、可解释重复、部分失败和完整性风险。

## 阶段 2：降低安装和使用门槛

**状态：核心已完成，收尾中**

0.3.x 版本需要用户准备 Node.js、npm、Git 和 Chrome。`v1-tauri` 分支已经把应用重写为基于 [Tauri](https://tauri.app) 的独立桌面应用：知乎登录改为内嵌窗口完成，后端用 [Bun](https://bun.sh) 编译成独立可执行文件随应用打包，普通用户不再需要安装 Node.js、Chrome 或运行任何命令。目标用户流程已经落地：

```text
下载应用
→ 打开应用
→ 点击开始登录
→ 确认保存位置
→ 开始导出
```

### 这一阶段的硬要求

- 不降低 Archive Core 的完整性标准；已完成——归档、去重、图片处理逻辑与 0.3.x 完全一致，只是登录和分页请求的底层实现从 Playwright 换成了内嵌 WebView；
- 登录仍在用户本机完成；已完成；
- 登录状态只保存在本机（由系统 WebView 管理）；已完成；
- 用户能够清除保存的登录数据；已完成——应用内置"退出登录"；
- 不因为桌面化引入云端账号、遥测或用户内容上传；已完成；
- macOS 和 Windows 至少完成干净环境测试。**未完成**——目前只在 macOS（Apple Silicon）上做过完整测试，Windows、Linux 和 Intel Mac 尚未验证。

### 收尾工作

- Windows / Intel Mac / Linux 平台测试；
- Apple 开发者签名，消除首次打开时的 Gatekeeper 警告；
- 合并 `v1-tauri` 到 `main`，正式发布 1.0.0（当前仍是预发布版本，见 [Releases](https://github.com/zhangyingfeng/zhi-dang/releases)）；
- 导出体验的暂停/继续能力仍在评估中，不阻塞 1.0 发布。

## 阶段 3：从归档生成个人网站

**状态：未来规划，需等待导出功能稳定**

当归档格式和导出流程足够稳定后，再规划从已有归档数据生成个人网站。

这一阶段的核心原则是：**网站生成器读取已经导出的本地归档，不重新访问知乎。**

可能包括：

- 从 `index.json`、Markdown 和本地图片生成静态网站；
- 回答和文章的独立阅读页面；
- 时间、类型、主题等内容导航；
- 本地搜索和排序；
- RSS、sitemap 等开放输出；
- 网站主题和展示配置。

网站生成与网站部署应分开设计。服务器、托管平台、DNS、SSL 和自定义域名等发布问题属于更靠后的可选能力，不应成为第一版网站生成器的前置条件。

## 更远期方向

当“归档”和“生成网站”都稳定后，再评估：

- 在个人网站或本地工作区中编辑和修改文章；
- 草稿、修订和发布流程；
- 文章版本历史和差异比较；
- 对作者长期知识、观点和写作过程进行结构化整理；
- AI 辅助检索、编辑、连接和创作；
- 由作者自己控制的个人写作模型。

这些方向属于长期愿景，目前不进入核心开发范围。详见 [VISION.md](VISION.md)。

## 范围控制

当前阶段默认不优先开发：

- 网站托管和服务器管理；
- 域名和 DNS 自动配置；
- CMS；
- 云同步；
- AI 自动分类和摘要；
- 个人写作模型训练。

新增功能进入实现前，应先回答一个问题：

> 这个修改是否首先提高了作者对自己内容的完整保存、长期可用性或使用门槛？

如果不是，应优先考虑延后。
